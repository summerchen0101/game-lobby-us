# 單一前端在 web/；base 讓建置在 web 內執行（同 Netlify UI 的 Base directory = web）
[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# React Router（BrowserRouter）：子路徑與重新整理導到 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
