syntax = "proto3";
package slot;
option go_package = "gitlab.ffglobaltech.com/us888/pluto/protos/slot";

message RobotGameInfo {
    int64 energy            = 1;
    int64 maxEnergy         = 2;
    int64 eventEnergy       = 3;
    int64 robotAttackEnergy = 4;
}
message egyptGameInfo {
    repeated string bingoSymbol     = 1;  // 消失圖標
    repeated string bugsBingoSymbol = 2;  // 飛行蟲 X2 圖標
}

message pveGameInfo {
    int64 checkpoint = 1;
}

message KingdomsInfo {
    int64 bossID        = 1;  // bossID
    int64 bossHp        = 2;  // boss 目前血量
    int64 bossTotalHp   = 3;  // boss 總血量
    int64 energy        = 4;  // 目前能量
    int64 energyMax     = 5;  // 最大能量
    string energySymbol = 6;  // 能量圖標
}

message BeachGameInfo {
    int64 multiplier = 1;  // 倍率
}

message PirateGameInfo {
    int64 big   = 1;
    int64 huge  = 2;
    int64 grand = 3;
}

message FishGameInfo {
    int64 big   = 1;
    int64 huge  = 2;
    int64 grand = 3;
}
message ShanghaiGameInfo {
    int64 multiplier = 1;  // 倍率
}
message Super777GameInfo {
    repeated Super777GameInfoData data = 1;
}
message Super777GameInfoData {
    int64 symbolCount = 1;  // 777 個數
    int64 symbolScore = 2;  // 777 倍率
}
message CandyGameInfo {
    int64 energy = 1;
}
message HotelGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message CatWomanGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message ScatterCountData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message FoxGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message ChristmasGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message DragonLegendGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 47 fortunes
message FortunesGameInfoData {
    int64 grand     = 1;  // grand 金額
    int64 major     = 2;  // major 金額
    int64 minor     = 3;  // minor 金額
    int64 mini      = 4;  // mini 金額
    int64 nowWCount = 5;  // 目前聚寶盆的數量
    int64 minWCount = 6;  // 聚寶盆超過此數開始出現期待
    int64 maxWCount = 7;  // 聚寶盆超過此數開始出現大獎
}

message FruitGameInfoData {
    int64 cherryCount = 1;  // 櫻桃連線累積數
}

// Slot 43 spicyGirl
message SpicyGirlGameInfoData {
    int64 remainingCherryTimes = 1;  // 櫻桃連線累積剩餘次數 (Fever 觸發條件)
    int64 remainingBellTimes   = 2;  // 鈴鐺連線累積剩餘次數 (Fever 觸發條件)
    int64 jp1Money             = 3;  // 彩池 1
    int64 jp2Money             = 4;  // 彩池 2
    int64 jp3Money             = 5;  // 彩池 3
}

// Slot 49 sambaGirl
message SambaGirlGameInfoData {
    int64 nowScatterCount                  = 1;  // 目前 scatter 的數量
    int64 minScatterCount                  = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount                  = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterSetting scatterSetting = 4;  // scatter 配置表
}
message ScatterSetting {
    int64 key   = 1;  // scatter 數量
    int64 count = 2;  // free game 數量
}

// Slot 50 thor
message ThorGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}
message ScatterInfo {
    int64 type                             = 1;  // 1: mg,  2: fg
    repeated ScatterSetting scatterSetting = 2;  // scatter 配置表
}

message pharaohTreasureGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}

message LuciferGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}

message HalloweenGameInfoData {
    int64 extra    = 1;
    int64 double   = 2;
    int64 multiple = 3;
}

message FishDraggonGameInfo {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
    // "EB": [15, 20, 30, 40, 50]
    map<string, FishDragonBonus> bonusValue = 5;
}
message FishDragonBonus{
    repeated int32 val = 1;
}

message AthenaGameInfo {
    int32 mini = 1;
    int32 minor = 2;
    int32 major = 3;
    int32 grand = 4;
}

message RuneStormGameInfo {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message ElementalMaskGameInfo {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message WestChiliGameInfo {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message PowerWealthGodGameInfo {
    
}

// Slot 51 dragonLegend2
message DragonLegend2GameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
// Slot 52 happy777
message Happy777GameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 45 GuanJianShou
message GuanJianShouData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 40 Santaizi
message SantaiziGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 249 cheerLeader
message CheerLeaderGameInfoData {
    int64 nowScatterCount                  = 1;  // 目前 scatter 的數量
    int64 minScatterCount                  = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount                  = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterSetting scatterSetting = 4;  // scatter 配置表
}
// Slot 238 anubis
message AnubisGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 54 highSpicyGirl
message HighSpicyGirlGameInfoData {
    int64 remainingCherryTimes = 1;  // 櫻桃連線累積剩餘次數 (Fever 觸發條件)
    int64 remainingBellTimes   = 2;  // 鈴鐺連線累積剩餘次數 (Fever 觸發條件)
    int64 jp1Money             = 3;  // 彩池 1
    int64 jp2Money             = 4;  // 彩池 2
    int64 jp3Money             = 5;  // 彩池 3
}
// Slot 55 slotMoney
message MoneyGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
// Slot 22 baseBall
message BaseBallInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 41 ninja
message NinjaData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message FoxGoldGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 57 temple
message TempleGameInfoData {
    int64 grand           = 1;  // grand 金額
    int64 major           = 2;  // major 金額
    int64 minor           = 3;  // minor 金額
    int64 mini            = 4;  // mini 金額
    int64 nowScatterCount = 5;  // 目前 scatter 的數量
    int64 minScatterCount = 6;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 7;  // scatter 超過此數開始出現大獎
}

message LokiGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 42 超八
message SuperEightGameInfoData {
    int64 jp1Money = 3;  // 彩池 1
    int64 jp2Money = 4;  // 彩池 2
    int64 jp3Money = 5;  // 彩池 3
}
message DiamondGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message XiyouGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message QinShiHuangGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message ThreeKingdomsHeroGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message TigerGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
// Slot 67 呂布
message LubuGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}
// Slot 67 呂布
message WaterGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 65 狂野水牛
message WaterCowGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
// Slot 66 冰火
message IceFireInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message FrogInfoData {
    int64 extra   = 1;
    int64 double  = 2;
    int64 mutiple = 3;
    int64 mini    = 4;
    int64 minor   = 5;
    int64 major   = 6;
    int64 grand   = 7;
}
message CatFireInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message PartyGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message TigerLuckyGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message SethGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}
// Slot 79 temple2
message Temple2GameInfoData {
    int64 grand           = 1;  // grand 金額
    int64 major           = 2;  // major 金額
    int64 minor           = 3;  // minor 金額
    int64 mini            = 4;  // mini 金額
    int64 nowScatterCount = 5;  // 目前 scatter 的數量
    int64 minScatterCount = 6;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 7;  // scatter 超過此數開始出現大獎
}

message DwarfGameInfoData {
    int64 grand           = 1;  // grand 金額
    int64 major           = 2;  // major 金額
    int64 minor           = 3;  // minor 金額
    int64 mini            = 4;  // mini 金額
    int64 nowScatterCount = 5;  // 目前 scatter 的數量
    int64 minScatterCount = 6;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 7;  // scatter 超過此數開始出現大獎
}
message AsgardGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}
message PhoenixGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message birdSecondGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message gladiGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message desperateraidGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message pandaGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// 150
message jaguarTempleGameInfo {
    int64 nowScatterCount = 1;  
    int64 minScatterCount = 2;  
    int64 maxScatterCount = 3;  
    int64 grand           = 4; 
    int64 major           = 5;  
    int64 minor           = 6;  
    int64 mini            = 7;  
}

message fishWishGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
    int64 grand           = 4;  // grand 金額
    int64 major           = 5;  // major 金額
    int64 minor           = 6;  // minor 金額
    int64 mini            = 7;  // mini 金額
}
message piggyBoxGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
    int64 grand           = 4;  // grand 金額
    int64 major           = 5;  // major 金額
    int64 minor           = 6;  // minor 金額
    int64 mini            = 7;  // mini 金額
}
message animeGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message wolfGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message WuKongGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message LionGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message cloverGameInfoData {
    int64 grand = 4;  // grand 金額
    int64 major = 5;  // major 金額
    int64 minor = 6;  // minor 金額
    int64 mini  = 7;  // mini 金額
}
message apolloGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
    int64 grand           = 4;  // grand 金額
    int64 major           = 5;  // major 金額
    int64 minor           = 6;  // minor 金額
    int64 mini            = 7;  // mini 金額
}
message WantedGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message LadyGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
    repeated ScatterInfo scatterInfo = 4;  // scatter 配置表
}

// Slot 98 麻辣火鍋
message SpicyHotPotGameInfoData {
    int64 jp1Money = 1;  // 彩池 1
    int64 jp2Money = 2;  // 彩池 2
    int64 jp3Money = 3;  // 彩池 3
}
message BlackKnightGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message chilliGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
    int64 grand           = 4;  // grand 金額
    int64 major           = 5;  // major 金額
    int64 minor           = 6;  // minor 金額
    int64 mini            = 7;  // mini 金額
}

message VampireGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}
message EldoradoGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message SeaDragonPrincessGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message SpiderSpiritInfoData {
    int64 extra    = 1;
    int64 double   = 2;
    int64 multiple = 3;
    int64 mini     = 4;
    int64 minor    = 5;
    int64 major    = 6;
    int64 grand    = 7;
}
message SpeedFireGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message PersianEmpireGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message DarkTempleInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message JurassicWorldGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message HadesInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 111 JungleKong
message JungleKongInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message YamaInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// Slot 109 GoldFinger
message GoldFingerGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message RoyalPokerGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message ArthurOmen {
    int64 now_ea = 1;  
    int64 min_ea = 2;  
    int64 max_ea = 3; 
}

// 119 大富翁
message MonopolyGameInfoData {
    int64 mini                                              = 1;
    int64 minor                                             = 2;
    int64 major                                             = 3;
    int64 grand                                             = 4;
}

message FrankensteinInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message GhostBustersGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message NightMarketKingGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
    int32 MiniMultiplier  = 4;
    int32 MinorMultiplier = 5;
    int32 MajorMultiplier = 6;
    int32 GrandMultiplier = 7;
}


message SamuraiGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
    int32 MiniMultiplier  = 4;
    int32 MinorMultiplier = 5;
    int32 MajorMultiplier = 6;
    int32 GrandMultiplier = 7;
}


// 122 勇者
message BraveGameInfoData {
  int64 nowScatterCount                        = 1;  // 目前 scatter 的數量
  int64 minScatterCount                        = 2;  // scatter 超過此數開始出現期待
  int64 maxScatterCount                        = 3;  // scatter 超過此數開始出現大獎
  map<string, LevelMulti> monsterLvlMultiMap   = 4;  // monster 賠率表，key: symbol, value: LevelMulti

  message LevelMulti {
    int64 level      = 1; // 等級
    int64 multiplier = 2; // 倍率
  }
}

// 126
message StarshipGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// 129
message BankHeistInfoData {
    int64 nowScatterCount                               = 1;  // 目前 scatter 的數量
    int64 minScatterCount                               = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount                               = 3;  // scatter 超過此數開始出現大獎
    map<string, BankHeistInfoDataMulti> bankHeistMulti  = 4;
}

message BankHeistInfoDataMulti {
    repeated int64 multiplier = 1;
}

// 131
message EmberFallGameInfoData {
  int64 nowScatterCount                        = 1;  // 目前 scatter 的數量
  int64 minScatterCount                        = 2;  // scatter 超過此數開始出現期待
  int64 maxScatterCount                        = 3;  // scatter 超過此數開始出現大獎
}

// 135
message ExplosionMineGameInfoData {
  int64 grand    = 1;
  int64 major    = 2;
  int64 minor    = 3;
  int64 mini     = 4;
  int64 ea_count = 5;
  int64 eb_count = 6;
  int64 ec_count = 7;
  int64 nowScatterCount = 8;  // 目前 EA EB EC 加總的數量
  int64 minScatterCount = 9;  
  int64 maxScatterCount = 10;
}

message LucyLuckGameInfoData {
  int64 nowScatterCount = 1;  // 目前 scatter 的數量
  int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
  int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message TankWarGameInfoData {
  int64 nowScatterCount = 1;  // 目前 scatter 的數量
  int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
  int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

message HerculesGameInfoData {
    int64 mini = 1;
    int64 minor = 2;
    int64 major = 3;
    int64 grand = 4;
}

// 139
message PitcherGameInfoData {
    int64 nowScatterCount                           = 1;  
    int64 minScatterCount                           = 2;  
    int64 maxScatterCount                           = 3;  
}

// 112
message GangsterWarGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// 144
message SteamPunkGameInfoData {
  int64 nowScatterCount = 1;  // 目前 scatter 的數量
  int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
  int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
  int64 mini            = 4;
  int64 minor           = 5;
  int64 major           = 6;
  int64 grand           = 7;
}

// 145
message PixelDungeonGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
}

// 148
message NinjaWarsGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
}

// 146
message BlessingDollsGameInfoData {
    int64 nowScatterCount            = 1;  // 目前 scatter 的數量
    int64 minScatterCount            = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount            = 3;  // scatter 超過此數開始出現大獎
}

message ArcanePortalsGameInfoData {
    int64 nowScatterCount = 1;  // 目前 scatter 的數量
    int64 minScatterCount = 2;  // scatter 超過此數開始出現期待
    int64 maxScatterCount = 3;  // scatter 超過此數開始出現大獎
}

// 151
message AtlantisGameInfoData {
    repeated EaBonusInfo eaBonusInfos = 1; // EA 有哪些Bonus能抽(原始值)

    message EaBonusInfo {
        int64 index          = 1; // 索引
        int64 bonus          = 2; // bonus
    }
}